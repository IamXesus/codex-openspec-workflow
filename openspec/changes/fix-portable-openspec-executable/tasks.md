<!-- openspec-review-contract:v3 -->
UI contract: none

## 1. <!-- openspec-wave:wave-1 --> Portable executable compatibility

- [x] 1.1 <!-- openspec-trace: requirements=REQ-POE-001; verification=Inspect the installed portable SKILL and bundled validator, then run focused tests proving every command uses the platform branch and no unconditional portable openspec.cmd remains. --> Replaced portable commands with one documented `<openspec>` platform placeholder; package and bundled validators now resolve `openspec` only on POSIX and prefer `openspec.cmd` with a PATHEXT-resolved fallback on Windows. Portable `rg` audit contains only the explicit Windows branch, Windows wrapper, and regression fixtures.
- [x] 1.2 <!-- openspec-trace: requirements=REQ-POE-002; verification=Run isolated resolver/content regressions with an injected empty developer inventory, a POSIX inventory containing only openspec, and Windows inventories containing openspec.cmd or PATHEXT-resolved openspec; all pass without consulting the real PATH. --> Existing test modules now create a temporary isolated executable inventory containing only extensionless `openspec`, prove `openspec.cmd` absent, and independently exercise both Windows outcomes. Focused Windows and WSL replays passed.
- [x] 1.3 <!-- openspec-trace: requirements=REQ-POE-004; verification=Run package metadata/policy tests and inspect README; package.json and package-lock.json both report 1.1.1, README retains external OpenSpec CLI 1.8.x and does not claim implicit installation. --> README now states the platform executable and separately installed CLI 1.8.x contract; package and root lock metadata plus receipt expectations report `1.1.1`. No dependency, tag, release, or publication was added.
- [x] 1.4 <!-- openspec-trace: requirements=REQ-POE-003; verification=In WSL/Linux temporary roots with an isolated PATH containing only a fake openspec executable, run scripts/install.sh dry-run, install, check, and rollback for codex and omnigent; both reach expected states and all writes remain inside rehearsal roots. --> WSL2 Ubuntu rehearsal passed for `codex` and `omnigent`: each isolated PATH exposed only its temporary `openspec`; dry-run was `ready`, install/check were `current`, rollback was `restored`, shared-root files were removed, and operation-specific roots were deleted. `*.sh text eol=lf` protects direct POSIX wrapper execution.
- [x] 1.5 <!-- openspec-trace: requirements=REQ-POE-001,REQ-POE-002,REQ-POE-003,REQ-POE-004; verification=Run full Python unittest discovery, both schema validations, architecture planning/apply/verify gates, Windows wrapper checks, WSL POSIX checks, strict OpenSpec validation, portable rg audit, and git diff checks; all applicable checks pass and promptfoo remains excluded because routing semantics are unchanged. --> Final `scripts/validate.ps1` passed 68 package/policy/bootstrap tests (one Windows filename skip), 60 requirement/gate tests, 4 architecture tests, five skill checks, both schema validations, lock metadata, and its PowerShell wrapper regression. Final inherited-PATH WSL replay passed the same 68 tests with four explicit environment/platform skips, 60 gate tests, and 4 architecture tests; focused WSL resolver/EOL tests passed 4/4. The inherited WSL PATH exposes mounted Windows npm shims without Linux `node`; the real-CLI decorators now use the platform resolver plus a bounded operational probe, and the exact three affected tests skip rather than fail. Architecture planning/apply/verify were READY, strict OpenSpec passed 4/4 changes, the custom gate passed, and `git diff --check` passed. Promptfoo was intentionally excluded because routing semantics did not change.
- [x] 1.6 <!-- openspec-review:wave --> Wave checkpoint: `/root/portable_final_review` reviewed the full current pending diff, identified one preliminary Medium in the cross-platform real-CLI test gate, and confirmed its operational-probe remediation plus permanent regression. Findings: High 0, Medium 0, Low 0. Verdict: PASS.

## 2. Release review

- [x] 2.1 <!-- openspec-review:final --> Independent full-diff review passed with High 0, Medium 0, Low 0 and confirmed that the Wave 1 review may satisfy this final checkpoint because it covered the complete remediated pending diff and no later material diff exists. Coverage: full pending diff; Requirements: REQ-POE-001, REQ-POE-002, REQ-POE-003, REQ-POE-004; Exclusions: real profile/package/CLI installation, test-host/server mutation, push/tag/GitHub release/deploy, promptfoo because routing semantics are unchanged; Reviewer: /root/portable_final_review
